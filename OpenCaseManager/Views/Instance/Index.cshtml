
<style>
    .nav-tabs > li.active > a, .nav-tabs > li.active > a:hover, .nav-tabs > li.active > a:focus {
        /*
        color: #fff;
        background-color: #9C1A5E;
        */
        color: #9C1A5E;
        background-color: #fff;
    }

    .nav-tabs > li.active > a, .nav-tabs > a:hover {
        text-decoration: underline;
    }
    .nav-tabs {
        display: flex;
        justify-content: center;
    }

    .divider {
        border-left: 6px solid green;
        height: 500px;
    }
</style>

@{
    ViewBag.Title = "Instance";
}

@Html.Partial("~/Views/PartialViews/Instruction.cshtml")

<div class="row tableDataRow">
        <wrapper>
    <div class="">
        <ul class="nav nav-tabs mb-3" id="tabs-tab" role="tablist">
            <li class="nav-item active">
                <a class="nav-link" id="tabs-test1-tab" data-toggle="pill" href="#tabs-test1" role="tab" aria-controls="tabs-test-page-1" aria-selected="false">Activities</a>
            </li>
            <div class="vl"></div>
            <li class="nav-item">
                <a class="nav-link" id="tabs-test2-tab" data-toggle="pill" href="#tabs-test2" role="tab" aria-controls="tabs-test-page-2" aria-selected="false">Timeline</a>
            </li>
        </ul>
        </wrapper>
        <div class="tab-content" id="tabs-tabContent">
            <div class="tab-pane fade in active" id="tabs-test1" role="tabpanel" aria-labelledby="tabs-test1-tab">
                @Html.Partial("~/Views/PartialViews/Activities.cshtml")
            </div>
            <div class="tab-pane fade in" id="tabs-test2" role="tabpanel" aria-labelledby="tabs-test2-tab"> 
                @Html.Partial("~/Views/PartialViews/Timeline.cshtml")
            </div>
        </div>
    </div>
 </div>


<script src="~/Scripts/instance.js"></script>

<script type="text/javascript">
    var webPortalType = "Instance";
    var instanceId = getParameterByName("id", window.location.href);

    function getParameterByName(name, url) {
        if (!url) url = window.location.href;
        name = name.replace(/[\[\]]/g, "\\$&");
        var regex = new RegExp("[?&]" + name + "(=([^&#]*)|&|#|$)"),
            results = regex.exec(url);
        if (!results) return null;
        if (!results[2]) return '';
        return decodeURIComponent(results[2].replace(/\+/g, " "));
    }
</script>
<script src="~/Scripts/task.js"></script>

